function reading(bookPages, pagesPerHour, allDays)
{
    totalTime = bookPages / pagesPerHour;
    hoursPerDay = totalTime / allDays;
    console.log(hoursPerDay);
}

reading(212,

    20 ,
    
    2)